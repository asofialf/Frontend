import { Routes } from '@angular/router';
import {PlanificationComponent} from "./modules/planification/components/planification/planification.component";

export const routes: Routes = [
  { path: '', component: PlanificationComponent },  // Ruta para la raíz del sitio
];
